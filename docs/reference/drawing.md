# Drawing results

`build_drawing(...)` and `Sheet.build()` return a `Drawing`. Its public methods cover
inspection, semantic editing, linting, and export.

::: draftwright.drawing.Drawing
    options:
      filters: public
